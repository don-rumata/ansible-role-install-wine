---
#--- Version section ---#

win_branch: stable
# win_branch: devel
# win_branch: staging

#--- Repo section ---#

win_gpg_key: https://dl.winehq.org/wine-builds/winehq.key
wine_repo_deb_key: '{{ win_gpg_key }}'
wine_repo_rpm_key: '{{ win_gpg_key }}'

# If you *NOT* use apt-cacher-ng or other caching proxy - select "https".
http_or_https: http
# http_or_https: https
